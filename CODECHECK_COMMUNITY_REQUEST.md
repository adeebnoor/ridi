# Community CODECHECK request

Publication / manuscript: **Performance and group-fairness audits leave AI allocations unidentified** (prepared for submission to *Nature*)

Code and reproducibility repository: https://github.com/adeebnoor/ridi

Requested check: independent execution of the locked EPSS natural-update analysis (`RIDI-CYBER-NATURAL-UPDATE-v1`). The package is deterministic and reports a machine-readable execution record and canonical numerical outputs. The requested scope includes the top-1,000 allocation turnover, RIDI, within-version controls, future-KEV recovery, AUROC comparison, and the identity-control analysis.

The repository contains `codecheck.yml`, reproducibility materials, a public submission snapshot, and a prospective EPSS protocol lock. One external executor has reproduced the canonical numerical values in an independent environment; that run is not being presented as a CODECHECK certificate.

I request an unaffiliated community codechecker and will respond promptly to any reproducibility or metadata requests. A formal certificate is not claimed unless and until the CODECHECK workflow is completed.
